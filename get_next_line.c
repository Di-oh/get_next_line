char *get_next_line(int fd)
{
	char	*buf;
	static char *str;
	int	letras;
 	
	letras = 0;
	while(1)
	{
		
		letras = read(fd, buf, BUFFERSIZE);
		if		
	} 
		return (NULL);
}

void	leer_y_guardar(fd);
{
	char *buf;

	char *buf;

}
